{
  "name": "reflexia-app",
  "version": "0.1.0",
  "description": "A new Celo blockchain project",
  "private": true,
  "scripts": {
    "build": "pnpm --filter web build",
    "dev": "pnpm --filter web dev",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "clean": "pnpm --filter web clean",
    "type-check": "pnpm --filter web type-check",
    "video:dev": "pnpm --filter preview-video dev",
    "video:build": "pnpm --filter preview-video build",
    "video:render": "pnpm --filter preview-video render"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "turbo": "^1.10.16",
    "typescript": "^5.2.2"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
